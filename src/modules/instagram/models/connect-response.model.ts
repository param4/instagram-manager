/**
 * Response returned when generating an Instagram OAuth authorization URL.
 */
export class ConnectResponseModel {
  /** The full Instagram OAuth authorization URL to open in a browser */
  authUrl: string;
}
