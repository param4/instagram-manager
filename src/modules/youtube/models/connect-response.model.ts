/**
 * Response returned when generating a Google OAuth authorization URL for YouTube.
 */
export class ConnectResponseModel {
  /** The full Google OAuth authorization URL to open in a browser */
  authUrl: string;
}
